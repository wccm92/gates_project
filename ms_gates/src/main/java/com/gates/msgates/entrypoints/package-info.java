/**
 * Entry points: inbound adapters that expose the application to the outside world.
 * Examples: REST controllers, Kafka consumers, RabbitMQ listeners, gRPC servers.
 * Translates external requests into use case calls.
 */
package com.gates.msgates.entrypoints;
