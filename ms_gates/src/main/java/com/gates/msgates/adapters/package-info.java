/**
 * Adapters: outbound adapters that communicate with external resources.
 * Examples: database repositories, HTTP clients, message producers, event publishers.
 * Implements output port interfaces defined in the domain use case layer.
 */
package com.gates.msgates.adapters;
